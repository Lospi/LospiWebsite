import 'package:flutter/material.dart';

class OuterSpaces extends StatelessWidget {
  const OuterSpaces({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text("Outer Spaces")),
    );
  }
}
